sudo: required
dist: trusty
services: docker
language: python
python: 3.5
cache:
  timeout: 600

jobs:
  include:

    - stage: integration-test
      os: linux
      install:
          - sudo apt-get -y install nodejs
      script:
          - cd tests/js && npm install && npm test
